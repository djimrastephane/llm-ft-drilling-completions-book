# Figures

- `cover.jpg` — book cover image, referenced by `_quarto.yml`
  (`cover-image`). **Not yet added.**
- `app_screenshot.png` — screenshot of the companion app once it exists,
  referenced from `index.qmd`. **Not yet added.**
- `diagrams/` — per-chapter pipeline and theory diagrams. See
  `diagrams/README.md`.

Until `cover.jpg` is added, `quarto render` will fail on the cover-image
reference in `_quarto.yml` — either add a placeholder image or comment
out that line while drafting.
